{
  "name": "ink",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "INK — a full-screen reactive GPU fluid simulation (WebGL2, Navier–Stokes). Drag to stir colourful ink.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
